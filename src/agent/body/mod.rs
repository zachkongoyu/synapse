pub (in crate::agent) struct Body {
    // toolbelt: ToolGateway,
    // executor: RuntimeEngine
}

impl Body {
    pub (in crate::agent) fn new() -> Self {
        Self {
            // toolbelt: ToolGateway::new(),
            // executor: RuntimeEngine::new(),
        }
    }

    pub (in crate::agent) fn perceive(&self) {
        todo!()
    }

    pub (in crate::agent) async fn act(&self) {
        todo!()
    }
}